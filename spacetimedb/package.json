{
  "name": "timer-counter",
  "license": "ISC",
  "type": "module",
  "scripts": {
    "spacetime:generate": "spacetime generate --lang typescript --out-dir ../src/lib/module_bindings --module-path .",
    "spacetime:publish": "spacetime publish --delete-data=on-conflict --yes timer-counter-6b3bt"
  },
  "dependencies": {
    "spacetimedb": "^2.0.3"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}