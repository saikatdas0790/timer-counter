---
# Plaintext vars committed to version control.
# Only the API token is sensitive; zone_id and account_id are public identifiers.

# Cloudflare Configuration
cloudflare_api_token: "{{ vault_cloudflare_api_token }}"
cloudflare_zone_id: "4250af23497bb7a306e4450a7709a997"       # saikat.dev zone
cloudflare_account_id: "21783118d91fac275d5bc116c37da809"    # Cloudflare account ID

# DNS record
dns_record_name: "timer-counter"          # subdomain (timer-counter.saikat.dev)
dns_record_content: "saikatdas0790.github.io"
dns_record_type: "CNAME"
dns_record_proxied: true
